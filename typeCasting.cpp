#include<iostream>
using namespace std;
int main (){
    /*
    int a = 'a';
    cout << a << endl;

    char ch = 98;
    cout << ch << endl;

    char ch1 = 123456;  // It may not give expected result because it exceeds char limits
    cout << ch1 << endl;
    */

    unsigned int a = 112;   // unsigned is used to store only +ve values if we store -ve value it will give unexpected result
    cout << a << endl;  
}